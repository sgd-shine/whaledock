# 合规证据按仓库原始字节做 SHA-256 绑定；禁止 Windows checkout 改写换行，也不把上游空白当仓库格式错误。
licenses/** -text -whitespace
compliance/** -text -whitespace
THIRD_PARTY_NOTICES.md -text -whitespace
package-lock.json -text -whitespace
# v0.10 的 fork patch、再分发源文件与固定 baseline 都按原始字节验 SHA；
# Windows core.autocrlf 不得把 LF/EOF 语义改写成 CRLF。
refork/dsh-ui/** -text -whitespace
context-poc/** -text -whitespace
lib/context-poc-baseline.json -text -whitespace
