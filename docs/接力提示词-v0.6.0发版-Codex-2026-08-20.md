# 接力提示词：v0.6.0 正式发版（签名资料配好后交 Codex，2026-08-20）

用法：SGD 把六个 GitHub secrets 配齐之后，新开一个 Codex 线程，把下面 ``` 之间整段贴进去。

现状（本提示词写作时已核实）：main 已推到 c6d18c9（version 0.6.0，未打 tag）；v0.5.1 已发布且本机已装；六个签名/公证 secret 待 SGD 配置；内部经营文档（电商四份、客服接待台草稿、值班机定向）已通过 `.git/info/exclude` 挡在公开仓库外（合规 ✅，不要动这个机制）。

---

```
你在鲸坞 WhaleDock 仓库接力，执行 v0.6.0 的正式发版与收尾。仓库在本机
/Users/shine/AI工作台/02_AI项目/02_产品实验室/30_桌面App/harness-desktop

【先读】AGENTS.md 全文；docs/v0.6交付与验收清单-2026-08-20.md；
docs/接力提示词-发布链-Codex-2026-08-20.md（本文件是它的续段，其授权与红线全部继续有效：
SGD 已授权代理执行 v0.6.0 的 tag/Release；审批门精确值用完即删；本地构建一律 ad-hoc，
正式签名公证只由 CI 产出；.p12/.p8/密码永不进仓库与任何输出；.git/info/exclude 里的
内部文档屏蔽机制保持原样，永不提交那些文件）。

【第 0 步：对账，逐项确认后才继续】
1. `gh secret list`：六个签名/公证 secret 必须齐全（MACOS_SIGN_CERTIFICATE / _PASSWORD /
   _IDENTITY、MACOS_NOTARY_KEY / _KEY_ID / _ISSUER_ID）。缺任何一个 → 停下，告诉 SGD 缺哪个。
2. c6d18c9（version 0.6.0）的三平台 CI 必须全绿；本机 `npm run smoke` 复跑一次 ALL PASS。
3. 问 SGD 一句：交付清单第三节 A–E 的真机验收过到什么程度了？
   - 若 A（装与切）、B（五个按钮）、E（文案）尚未过 → 先陪他把这三组补完（fixture 现做），
     发现问题按「文案/提示词零成本改、逻辑小修当场修+smoke+CI、大问题停下汇报」处理；
   - C（叫醒观感）与 D-7（NFC/NFD）若他想留到公证版装好后再看，允许，记入发版后清单。
4. 若 docs/侦察-dsh-rc7-*.md 还不存在，rc.7 侦察排在发版收尾之后做（第 5 步），不阻塞发版。

【第 1 步：打 tag 发 v0.6.0】
- tag v0.6.0，走既有 Release 流程；审批门 WHALEDOCK_BUNDLED_COMPLIANCE_APPROVAL 按精确
  tag + 产物集 SHA-256 临时设置，Release 与八项资产回读确认后立即删除并回读为空。
- 盯 workflow 日志逐项确认：证书导入成功、签名为 Developer ID + Hardened Runtime（不再是 ad-hoc）、
  notarize 提交并 Accepted、staple 成功。任何一步失败：不硬发，贴完整错误，修完再来。
- Release 说明（中文）要点：工作台包一段话（拖装/一键切/五按钮）；「本版起已签名并经 Apple 公证，
  下载后双击直接打开」；老版本升级提示；旧版救急命令保留一行。

【第 2 步：对下载产物做公证验收（核心闸门）】
- 从 GitHub 下载正式 Release 的 dmg 与 zip（不是本地构建物），逐项核对 SHA256SUMS。
- 给 dmg 打 quarantine 属性后安装：预期【零提示直接打开】。
- `spctl -a -vv` 与 `xcrun stapler validate` 对 .app 回读，输出原文进汇报。
- 仍出现任何 Gatekeeper 提示 = 发布失败：撤下有问题的结论口径，回查公证日志，修完重验。
  不许把它写成「预期摩擦」。

【第 3 步：文档口径更新（三处，改完提交，提交信息英文）】
1. README 安装节：改成「已公证、双击即开」，删掉/降级旧的绕行说明。
2. docs/macOS-安装与已损坏提示.md：中英日三段顶部各加一句「v0.6.0 起已公证，本文适用于旧版本」。
3. v0.5.0 与 v0.5.1 的 Release 页各补一行「建议升级 v0.6.0（已公证，零提示安装）」。

【第 4 步：把 SGD 本机升到 v0.6.0】
- 先开一次现装的 v0.5.1：应出现「发现新版 0.6.0」提醒（macOS 更新器只提醒不安装是设计），
  把提醒记录/截图下来——这是挂了很久的更新提醒验收项。
- 用第 2 步已核对校验和的 Release dmg 手动覆盖安装 /Applications/WhaleDock.app。
- 复核：mdfind 'kMDItemCFBundleIdentifier == "com.sgd.whaledock"' 只剩 /Applications 一个；
  codesign --verify --strict 通过；启动、拉起 dsh、切一次工作台、退出干净。
- 顺手陪 SGD 补掉验收清单里留到公证版的项（C 叫醒观感、D-7 NFC/NFD、E 未完成项）。

【第 5 步：rc.7 侦察（若第 0 步确认还没做）】
原样执行 docs/接力提示词-2026-08-18.md 的「任务 B」：不升级、不动版本锁、不改生产代码，
只产出 docs/侦察-dsh-rc7-2026-08-20.md，结论「升 / 不升 + 理由」。写完就停。

【明确不做】
- 不实现「客服接待台」（docs/ 里那份 v0.1 草稿属策划线待审计事项，等 SGD 与策划线程拍板后另开接力）。
- 不开始 v0.7 的任何内容；不动 dsh 版本锁；不写 ~/.dsh；不提交 .git/info/exclude 屏蔽的任何文档。

【汇报】每步单独汇报（做了什么/发现什么/怎么修/smoke 与 CI 实际结果）。最终交付：
Release 链接、公证证据（workflow 日志关键行 + spctl/stapler 输出）、quarantine 零提示验收结果、
文档三处修改的 diff 摘要、本机安装复核结果、验收清单 A–E 最终勾选状态、侦察报告链接、
以及仍需 SGD 人工完成的事项（托盘 20 张正式素材、短视频台 theme/pet、Windows 真机验证）。
全部做完停在这里。
```
