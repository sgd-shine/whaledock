# 接力提示词：v0.6「工作台包」实现阶段（2026-08-19）

用法：新开一个 Claude Code 线程，把下面 ``` 之间的整段原样贴进去。

本提示词的整合来源：`docs/开发方案-v0.6-工作台包.md`（需求唯一来源）+ SGD 2026-08-19 对 E 节全部问题的拍板 + 仓库对账结果（v0.5.1 已提交未打 tag）+ 团队值班机讨论落定的两条通用性输入。与 `docs/接力提示词-2026-08-18.md` 任务 C 冲突之处（如社区包目录），一律以 v0.6 方案为准。

---

```
你现在接手鲸坞 WhaleDock 的 v0.6「工作台包」实现。仓库在本机
/Users/shine/AI工作台/02_AI项目/02_产品实验室/30_桌面App/harness-desktop

【第一件事：先读，不要动手】
- AGENTS.md 全文（硬性约束 1–9、已知的坑、汇报要求）——一条都不许放松
- docs/开发方案-v0.6-工作台包.md 全文——这是本次实现的唯一需求来源。
  A 节的字段表（A-5 ~ A-10）与安全校验表（A-11）按行实现；C 节按表实现；D 节风险逐条对照。
  它与更早的 docs/接力提示词-2026-08-18.md 任务 C 或第三阶段方案有出入时，以 v0.6 方案为准
  （例：社区包目录 v0.6 不开，顺延 v0.7）。
- lib/pets.js、lib/themes.js（v0.5 声明式包模型，复用其解析器与校验风格，不复制代码）
- lib/workspaces.js（v0.4 工作区串行事务 + journal，重工作台切换必须复用，一行事务代码不新写）
- lib/backend.js（createDshPromptAdapter，actions 提交的唯一通道）
- lib/events.js（derivePetState() 五态推导已存在——P2 的工作在「表达」不在「感知」，汇报不许吹成从零做感知）

【SGD 已拍板（2026-08-19），照此执行，不要再问】
E-0 三项（方案里已记录）：轻/重工作台区分通过；装=拖进窗口一步、切=点一下；
底座通用性三条规则（A-13）优先级高于任何功能需求。
E-1 五项，本次全部落定：
1. 五个纸面包：要做，开工第一批就是它（见批次 1）。
2. B-4 五个按钮提示词：按现稿直接用，原样入包。SGD 真机试用后再改字，本次不改内容。
3. skills.json：第一版发空的（{"skills": []}），UI 显示「这个工作台没有推荐 skill」。
4. 叫醒阶梯 30 秒档（Dock 持续弹跳 / 任务栏闪烁）：默认开；设置里单独开关 + 安静模式总开关，
   用户一动立即全停，每个待确认项只叫一遍。
5. 托盘 20 个图标：先用程序合成的占位角标跑通全部逻辑与验收，正式素材单独一批，不阻塞。

另有两条通用性输入（来自团队值班机讨论，docs/探讨-团队共享值班机-定向结论-2026-08-19.md；
不是新功能，只是设计时别写死）：
- actions 的数据与渲染保持分离——按钮清单将来可能在移动/远程形态被复用成「快捷指令面板」。
- workspace.json 的 folders 语义保持行业无关——将来可能出现「收件箱」类约定目录，不为短视频包特化。

【批次 0：状态对账（先做，单独汇报）】
1. 对账 v0.5.1 发版状态：本地 git tag 目前只到 v0.5.0，v0.5.1 代码已在提交 2bded95 里。
   查 GitHub 上 v0.5.1 的 tag 与 Release 是否已存在且八项资产齐全：
   - 未完成 → 先原样执行 docs/接力提示词-2026-08-18.md 的「任务 A」把 v0.5.1 发出去。
     验收核心：给 dmg 打上 com.apple.quarantine 后重装，提示必须是「无法验证开发者」而不是「已损坏」。
     审批门变量按精确 tag + SHA-256 临时设置，Release 确认后立即删除并回读为空。
     任务 A 没走完，不进批次 2。
   - 已完成 → 一句话汇报现状即可，跳过。
2. 检查六个签名/公证 secret（MACOS_SIGN_CERTIFICATE/_PASSWORD/_IDENTITY、
   MACOS_NOTARY_KEY/_KEY_ID/_ISSUER_ID）是否已配置：配了则 Release 自动走公证通道；
   没配就如实记入「仍需 SGD 人工完成」，不催、不代办（Apple 账号注册与付费必须 SGD 本人）。
3. 把 docs/ 里未跟踪的鲸坞产品文档提交入库（英文提交信息）：
   开发方案-v0.6-工作台包.md、探讨-企业级多用户与工作台包-2026-08-19.md、
   提示词-v0.6工作台分支-新线程.md、接力提示词-v0.6实现-2026-08-19.md（本文档）。
   ⚠ 以下内容是 SGD 的内部经营资料，这个仓库是公开的，绝对不要提交，保持未跟踪：
   电商工作台相关四份文档（开发方案-电商工作台v3 / 商品主数据表结构 / 代发助手两份）、
   商品主数据-录入模板.xlsx、探讨-团队共享值班机-定向结论-2026-08-19.md、.claude/ 目录。
   是否迁走由 SGD 自己决定，你只负责不让它们进 git。
4. rc.7 侦察是另一个独立任务（docs/接力提示词-2026-08-18.md 任务 B），本次不做，也不被它阻塞：
   v0.6 全程锁 0.1.0-rc.6，agent.cordis.yml 只做「检测 + 显示」，不宣称接通。

【批次 1：五个纸面包（半天，只写 JSON，不写实现代码）】
按 v0.6 方案 A-13 的表，给 律师·合同初审台 / HR·招聘筛选台 / 客服·话术台 / 教师·备课台
各写一版完整纸面包（manifest + workspace + actions + onboarding；短视频台已在 B 节，不重写）。
产出 docs/纸面包验证-2026-08-19.md，逐包记录：六个原语哪里够用、哪里撞墙。
处理规则：撞墙但能用「将来加一个可选字段」解决的（A-13 已预判的输入表单/批量/多步三个缺口即此类），
记录后继续开工，不改 schema；只有发现「六原语结构性不够用、必须现在改 v1 schema」才停下来等 SGD，
并把问题用大白话写清楚。

【批次 2：P0-1 纯解析层】
lib/workbenches.js（纯 Node，禁止 require Electron）+ test/workbenches-smoke.js 接进统一 smoke。
- A-5 ~ A-10 逐字段实现：类型、上限、回落、拒绝理由全部按表，整包拒绝只有四种情况，
  其余一律「字段回落，包还能用」；被拒的包进 skipped[] 不拖垮列表。
- A-11 十条安全校验逐条实现并逐条写 smoke：目录名、..、绝对路径、符号链接、realpath 逃逸、
  ~/.dsh 保护（字面+realpath 两轮）、大小上限、64 包上限、静态断言不执行（无动态 require/eval/
  new Function/child_process）、静态断言不碰 cordis。
- fixture 三连：正常包 / 缺一半字段的包 / 恶意包（..、绝对路径、符号链接指向 ~/.dsh、超大文件），
  断言结果分别是 正常加载 / 回落加载 / 明确拒绝。
- A-13 规则一：未知字段一律忽略、只记灰色提示，smoke 里放一个「带未来字段的包」直测。

【批次 3：P0-2 Electron 薄层（轻工作台先通）】
- 扫描两个根：<安装目录>/assets/workbenches/（builtin）与 userData/workbenches/（user），
  id=目录名，builtin: 与 user: 前缀并存不覆盖。
- 拖入安装：文件夹拖进窗口 = 原样复制到 userData/workbenches/ 后重扫。只复制，不解压、
  不执行、不联网、不改源文件夹。切换器里可「移除」（只删副本）。
- 切换三入口：窗口左上角常驻工作台按钮、托盘菜单、⌘⇧1~9 切前九个 + ⌘⇧0 回上一个。
- 轻工作台（无 workspace.json）：点一下即切，换主题（id 用 wb:<包id>）、宠物、按钮栏，后端不重启，
  不弹任何确认。
- 设置窗「工作台」区：列表、切换、打开包目录（openUserResourceDir 白名单加 'workbenches'）、
  skills 清单展示 + 复制安装命令按钮（永不自动安装），栏顶固定警示句按 A-9 原文，不可关闭。
- onboarding.md 按 A-10 纯文本显示（textContent 按行写入，# 行加大加粗、- 行成条目，不解析
  Markdown/HTML、不生成链接）。
- actions 按钮：走 createDshPromptAdapter（仅 loopback + packageProven + 单文本 + unknown 不重试），
  点击后 5 秒禁用自身，unknown 时按 D-4 文案提示；confirm:true 先弹确认。提示词是死文本，
  不做变量替换、不与输入框内容合并。smoke 里造 unknown 假适配器，断言不自动重试、不重复发。

【批次 4：P0-3 重工作台】
- 写了 workspace.json 的包：首次启用弹一次 A-3 样式的确认卡（作者/许可证/要建的文件夹/要换的东西/
  重启提示），之后再切不弹。
- 建目录三铁律：只新建不覆盖（说明.md 已存在一个字不动）、没有任何删除用户文件的代码路径、
  落点过 canonicalWorkspace() + assertWorkspaceNotForbidden()（字面+realpath 两轮，拒绝时连文件夹都不建）。
- 切换完整复用 v0.4 工作区串行事务 + journal，失败自动回滚；外部 attach 模式拒绝切换时要有
  专门的解释文案（补 reportWorkspaceSwitchError 的工作台场景）。
- 真机量一次切换耗时，>5 秒必须全程进度条。
- 落点：<文档目录>/鲸坞工作台/<root>/。

【批次 5：P1 短视频创作台内置包】
- 放 assets/workbenches/短视频创作台/，内容照 B 节：workspace.json 四文件夹带 readme、
  actions.json 五个按钮、五段提示词全文原样入包（死文本，一字不改）、onboarding.md 按 B-5、
  skills.json 发空、agent.cordis.yml 不放（顺便验证缺字段回落）、theme/pet 先跟随全局（素材后补不阻塞）。
- 写 docs/工作台包制作指南.md：风格对齐 v0.5 宠物包制作指南，包含 A-13 的「包作者契约」
  （我们的四条承诺 + 对作者的三条要求）与「zip 双击解压 + 拖入」的分发说明。

【批次 6：P2 托盘五态 + 叫醒阶梯】
- 托盘直接消费 derivePetState() 输出，与宠物窗同一数据源；五态表现按 C-3 表（思考中 2fps 图片切换）。
- 连不上事件流 = 第六种显示（灰态「连接不上，正在重试」），不猜、不装作空闲；
  断流兜底：每 5 秒 session.list 只读轮询，连续 3 次失败转灰态。
- 叫醒阶梯按 C-4 表：0 秒（托盘角标+Dock 数字+系统通知+宠物晃动）→ 8 秒（宠物窗临时置顶摆动一次，
  不抢焦点）→ 30 秒（覆盖式二次通知 + dock.bounce('critical') / flashFrame，本档默认开）。
  提示音 shell.beep() 默认关。用户一动全停；按 approval/question 的 id 去重，每项只叫一遍，
  轮询重连重见同一项不再叫。三件永远不做的事（不抢焦点/不遮挡/不循环响铃）写进代码注释。
  每层可单独关 + 「安静模式」总开关（只留托盘图标）。
- 图标先用程序合成占位角标（现有图标+角标合成），20 个正式 PNG 素材记入待办，不阻塞验收。
- 新增 dsh --profile <name> --dump-config 只读探测：只读子进程、3 秒超时、只读 stdout、
  失败当「没有这个信息」绝不阻断启动（这是代码里对 --dump-config 的第一处引用）。

【硬性要求（逐条不许放松，违反即返工）】
1. lib/workbenches.js 禁止 require Electron，必须能被纯 Node 的 test/smoke.js 加载。
2. 不新增任何运行时依赖；devDependencies 仍只有 electron + electron-builder。
3. 包里只有数据：绝不 require、绝不 eval、绝不 spawn 包内任何东西。
4. agent.cordis.yml 只做存在性与路径校验（普通文件、非符号链接、≤256KiB、realpath 在包内），
   内容一个字节不读进解析器，不引入 YAML 库；UI 只能写「已检测到，尚未接通」。
5. skills.json 只展示 + 复制按钮，永不自动安装，无任何 spawn 路径。
6. 包内路径拒 ..、绝对路径、符号链接逃逸，字面与 realpath 两层都拒；拒任何指向 ~/.dsh 及其
   后代/链接目标的路径。
7. 任何一项解析失败 → 整体降级到默认工作台并如实报错，不静默吞掉、不留半启用中间态。
8. actions 提交只走 v0.4 prompt adapter：仅 loopback + packageProven 精确等于 0.1.0-rc.6 +
   单条纯文本 + unknown 绝不自动重试。
9. 每次改动跑 npm run smoke 必须 ALL PASS；test/workbenches-smoke.js 至少覆盖：路径逃逸拒绝、
   缺字段回落、agent.cordis.yml 不被解析、skills 不自动安装、actions 模板不被当代码执行、
   未知字段忽略、Windows 大小写撞车（注入 platform:'win32' 直测）。
10. 提交信息用英文；git 邮箱用 281213992+sgd-shine@users.noreply.github.com（本机已配好，确认一次）。
11. 不动 dsh 版本锁；不写入/迁移/清理 ~/.dsh；不买证书、不用付费服务、不碰 SGD 的 Apple 账号。
12. 不把「CI 构建成功」写成「真机验证通过」。

【不要做的事】
- 不开 community-workbenches/ 社区包目录（v0.7 连同审核流程一起做）
- 不做 zip/.whaledock 一键安装（v0.7，需专门处理 zip-slip）
- 不做提示词变量/模板引擎（{{变量}} 原样发出）、不渲染 Markdown、不解析 HTML
- 不碰 Cordis 内部 API（smoke 静态断言兜底）
- 不改 B-4 五个提示词的内容
- 不提交电商工作台文档、xlsx、值班机定向结论、.claude/（见批次 0 第 3 条）
- 本次不做 rc.7 侦察、不做扫码远程、不做引擎瞭望塔

【汇报格式】
每完成一个批次，单独汇报几句：做了什么、发现什么、怎么修、npm run smoke 实际通过数。
批次之间不许合并汇报。批次 1 的纸面包结论要用大白话单独说清「撞没撞墙、撞在哪」。

【最终交付】
- 改动文件清单；A-11 十条安全校验的逐条证据（对应 smoke 用例名）；纸面包验证记录链接；
  五态与叫醒阶梯的真机现象描述；包体积变化；CI/Release 链接；
- 一张「SGD 人工验收清单」，至少含：真机拖装一个包、轻/重工作台各切一次（重的量耗时）、
  五个按钮各点一遍、把 01_选题库 改名后再点按钮看 agent 是否老实说找不到（D-3）、
  叫醒阶梯的主观感受（D-5，无自动化可替代）、macOS 中文目录名 NFC/NFD 实验（D-7，连宠物包一起查）、
  skills 空清单的文案检查、切换器里坏包的灰色「未加载：原因」显示；
- 以及仍需 SGD 人工完成的事项（如 Apple 账号/secret 未配则列明）。
做完停在这里等 SGD 验收，不要自行开始 v0.7。
```
